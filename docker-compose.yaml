services:
  veo-gen-bot:
    build: .
    container_name: veo-gen-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data/database.db:/app/database.db
      - ./storage:/app/storage
      - ./service-account.json:/app/service-account.json:ro
